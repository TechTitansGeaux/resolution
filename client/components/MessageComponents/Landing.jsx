import { React, useState } from 'react';

const Landing = (props) => {

  return (
    <div>
      <p> {'Welcome to Meme Messenger! Start up a conversation by clicking start conversations or see your previous conversations by clicking all conversations. '}</p>
    </div>
  );

};

export default Landing;
